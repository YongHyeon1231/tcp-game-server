syntax = "proto3";

package game;

// 게임 생성 핸들러 payload
message CreateGamePayload {
    int64 timestamp = 1; // 게임 생성 시간
}

// 게임 참가 핸들러 payload
message JoinGamePayload {
    string gameId = 1;
    int64 timestamp = 2;
}